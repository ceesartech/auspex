# Apache Airflow — runs inside Docker in this project.
# Install this file ONLY if you need to run Airflow DAGs outside Docker,
# e.g. for IDE code-completion or running individual DAG scripts locally.
#
# Usage:
#   pip install -r requirements-airflow.txt
#
# Note: Airflow has 500+ transitive dependencies and takes ~5 minutes to install.
# It is intentionally excluded from requirements.txt so local setup stays fast.

apache-airflow==2.10.3
apache-airflow-providers-postgres==5.13.0
apache-airflow-providers-redis==3.8.0
