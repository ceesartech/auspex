apiVersion: v1
kind: Service
metadata:
  name: betting-system-db
  namespace: betting-system
  labels:
    app: postgres
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
