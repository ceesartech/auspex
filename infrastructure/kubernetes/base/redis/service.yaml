apiVersion: v1
kind: Service
metadata:
  name: betting-system-redis
  namespace: betting-system
  labels:
    app: redis
spec:
  type: ClusterIP
  selector:
    app: redis
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
    name: redis
