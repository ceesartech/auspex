apiVersion: v1
kind: Service
metadata:
  name: betting-api
  namespace: betting-system
  labels:
    app: betting-api
spec:
  type: ClusterIP
  selector:
    app: betting-api
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
