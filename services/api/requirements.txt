# FastAPI and ASGI
fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9

# Database
sqlalchemy==2.0.27
psycopg2-binary==2.9.9
alembic==1.13.1

# Redis
redis==5.0.1

# Authentication
PyJWT==2.8.0
bcrypt==4.2.0

# Validation
pydantic==2.6.1
pydantic-settings==2.1.0

# Celery
celery==5.3.6

# Monitoring
prometheus-client==0.20.0

# ML (for model loading)
joblib==1.3.2
numpy==1.26.4

# Testing
pytest==8.0.1
pytest-asyncio==0.23.4
httpx==0.27.0
# powerball.com's CDN serves brotli-encoded responses from some edge nodes
# regardless of Accept-Encoding — httpx needs this to decode them
# (lottery_live jackpot fetch renders garbage without it).
brotli==1.1.0

# Utilities
python-dotenv==1.0.1
